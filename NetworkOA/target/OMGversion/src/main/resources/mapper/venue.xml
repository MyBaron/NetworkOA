<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.Dao.venueDao">
    <select id="findOneVenue" parameterType="String" resultType="cn.Entity.venueJavaBean">
        SELECT * FROM venue where venue_name=#{venue_name}
    </select>

    <select id="findAllVenue"  resultType="cn.Entity.venueJavaBean">
        SELECT * FROM venue
    </select>

    <update id="changeMessage" parameterType="cn.Entity.venueJavaBean">
      UPDATE venue  set venue_open=#{venue_open},venue_address=#{venue_address},venue_phone=#{venue_phone},
      venue_total=#{venue_total},
      venue_begin=#{venue_begin},venue_close=#{venue_close},venue_price=#{venue_price}
      where venue_id=#{venue_id}
    </update>

</mapper>